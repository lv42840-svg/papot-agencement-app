name: Quote line reorder temporary

on:
  push:
    branches:
      - feat/quote-line-reorder

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/quote-line-reorder
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: python scripts/patch-quote-line-reorder.py
      - run: npx prettier --write src/lib/quotes/mutations.ts src/components/quote-structured-lines-editor.tsx tests/quote-line-reorder.test.ts
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test -- tests/quote-line-reorder.test.ts tests/quote-line-duplication.test.ts tests/quote-lines-editor.test.ts
      - name: Commit reorder
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/quotes/mutations.ts src/components/quote-structured-lines-editor.tsx tests/quote-line-reorder.test.ts
          git commit -m "feat(quotes): deplacer les ouvrages"
          git push
