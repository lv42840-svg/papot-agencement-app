name: Quote line duplication temporary

on:
  push:
    branches:
      - feat/quote-line-duplication

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/quote-line-duplication
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: python scripts/patch-quote-line-duplication.py
      - run: npx prettier --write src/lib/quotes/mutations.ts src/components/quote-structured-lines-editor.tsx tests/quote-line-duplication.test.ts
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test -- tests/quote-line-duplication.test.ts tests/quote-lines-editor.test.ts
      - name: Commit duplication
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/quotes/mutations.ts src/components/quote-structured-lines-editor.tsx tests/quote-line-duplication.test.ts
          git commit -m "feat(quotes): dupliquer un ouvrage"
          git push
