name: Temporary Windows Installer Fix 47

on:
  push:
    branches: [test/windows-installer-fix-47]
    paths:
      - .github/workflows/tmp-windows-installer-fix47.yml

permissions:
  contents: read

jobs:
  build-windows-installer:
    runs-on: windows-latest
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"
      NEXT_TELEMETRY_DISABLED: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run desktop:dist -- --publish never
      - name: List installer output
        shell: pwsh
        run: Get-ChildItem -Path release -Recurse | Select-Object FullName, Length
      - uses: actions/upload-artifact@v4
        with:
          name: PAPOT-AGENCEMENT-Windows-Test-Fix47
          path: release/*.exe
          if-no-files-found: error
          retention-days: 7
