name: Temporary Library PostgreSQL Formatter

on:
  push:
    branches: [arch/library-postgres-cutover]
    paths:
      - .github/workflows/tmp-format-library-postgres.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: arch/library-postgres-cutover
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write src/lib/library/legacy-snapshot.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/library/legacy-snapshot.ts
          git commit -m "Format Library PostgreSQL cutover"
          git push origin HEAD:arch/library-postgres-cutover
