name: Quote heading duplication temporary

on:
  push:
    branches:
      - feat/quote-heading-duplication

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/quote-heading-duplication
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: python scripts/patch-quote-heading-duplication.py
      - run: npx prettier --write src/lib/quotes/mutations.ts src/components/quote-structured-lines-editor.tsx tests/quote-heading-duplication.test.ts
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test -- tests/quote-heading-duplication.test.ts tests/quote-heading-reorder.test.ts tests/quote-line-duplication.test.ts tests/quote-lines-editor.test.ts
      - name: Commit heading duplication
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/quotes/mutations.ts src/components/quote-structured-lines-editor.tsx tests/quote-heading-duplication.test.ts
          git commit -m "feat(quotes): dupliquer titres et sous-titres"
          git push
