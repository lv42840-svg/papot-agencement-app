name: Quote screen format

on:
  push:
    branches:
      - feat/quote-desktop-screen

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/quote-desktop-screen
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write src/components/quote-workspace.tsx tests/quote-draft.test.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/quote-workspace.tsx tests/quote-draft.test.ts
          git commit -m "Format quote desktop screen"
          git push origin HEAD:feat/quote-desktop-screen
