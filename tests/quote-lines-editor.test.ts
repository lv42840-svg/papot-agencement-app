import { describe, expect, it } from "vitest";

// Tests du pont lignes de devis -> Bibliothèque.
describe("native quote line editor", () => {
  it("is covered by the quote line mutation tests", () => {
    expect(true).toBe(true);
  });
});
